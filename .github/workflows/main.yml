name: krishnarestart

on: 
  push:
    branches:
      - main
      - feature
jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build completed"
      - run: echo "verygood learning"
